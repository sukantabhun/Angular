import { AlternateIfDirective } from './alternate-if.directive';

describe('AlternateIfDirective', () => {
  it('should create an instance', () => {
    const directive = new AlternateIfDirective();
    expect(directive).toBeTruthy();
  });
});
